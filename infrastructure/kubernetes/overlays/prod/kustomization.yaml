apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

namespace: quantumalpha

patches:
  - path: configmap.yaml

replicas:
  - name: data-service
    count: 3
  - name: ai-engine
    count: 3
  - name: risk-service
    count: 3
  - name: execution-service
    count: 3
  - name: web-frontend
    count: 3

images:
  - name: ${ECR_REGISTRY}/quantumalpha/data-service
    newTag: prod-latest
  - name: ${ECR_REGISTRY}/quantumalpha/ai-engine
    newTag: prod-latest
  - name: ${ECR_REGISTRY}/quantumalpha/risk-service
    newTag: prod-latest
  - name: ${ECR_REGISTRY}/quantumalpha/execution-service
    newTag: prod-latest
  - name: ${ECR_REGISTRY}/quantumalpha/web-frontend
    newTag: prod-latest
